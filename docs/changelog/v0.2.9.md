# v0.2.9

## Bug Fixes

- Fix Linux/macOS client install — enable single-file publish so the install script copies one self-contained binary instead of just the native host (which failed with "does not exist: EchoHub.Client.dll")
- Fix notification sound sometimes not playing - the sound may be delayed for a few seconds if a sound is already playing, but it should now always play eventually instead of being skipped entirely
